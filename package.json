{
  "name": "doodle-chat-challenge",
  "private": true,
  "description": "Monorepo wrapper for the Doodle frontend challenge submission.",
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  },
  "scripts": {
    "install:all": "npm install --prefix apps/api && npm install --prefix apps/web",
    "dev": "concurrently -k -n api,web -c blue,green \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "npm --prefix apps/api run dev",
    "dev:web": "npm --prefix apps/web run dev",
    "build": "npm run build:web",
    "build:api": "npm --prefix apps/api run build",
    "build:web": "npm --prefix apps/web run build",
    "lint": "npm run lint:web",
    "lint:api": "npm --prefix apps/api run lint",
    "lint:web": "npm --prefix apps/web run lint",
    "test": "npm run test:web",
    "test:web": "npm --prefix apps/web run test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
